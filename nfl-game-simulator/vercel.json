{
  "buildCommand": "cd frontend && npm install && npm run build && cp -r ../data dist/data",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "rewrites": [
    { "source": "/((?!data/).*)", "destination": "/index.html" }
  ]
}
